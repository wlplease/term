version: "3.7"
services:
  liveterm:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - ./src:/home/node/app/src
    command: yarn dev
    environment:
      - NODE_ENV=development
